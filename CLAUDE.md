# Claude Project Entry Point

Read and follow `AGENTS.md` before working in this repository.

The current phase is planning-only Stage A. In particular:

- do not create authenticated clients or execution code;
- do not touch the HYPE repository or `/opt/bybit-rev`;
- do not add runtime dependencies until the Stage A plan is reviewed;
- treat `PROJECT_STATUS.md` and repository research artifacts as authoritative.
